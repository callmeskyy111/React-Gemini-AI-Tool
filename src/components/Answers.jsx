import React from "react";

function Answers({ ans, key }) {
  console.log(`Ans: ${ans}`, `Key: ${key}`);
  return (
    <div>
      {ans}
    </div>
  );
}

export default Answers;

// How to learn Typescript in a week as someone who knows the basics pf React?